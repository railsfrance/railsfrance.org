%ul
  - if controller_name != 'sessions'
    %li
      = link_to "Se connecter", new_session_path(resource_name), :class=> "buttonBlue"
  - if devise_mapping.registerable? && controller_name != 'registrations'
    %li
      = link_to "S'enregistrer", new_registration_path(resource_name), :class=> "buttonBlue"
  - if devise_mapping.recoverable? && controller_name != 'passwords'
    %li
      = link_to "Mot de passe oublié ?", new_password_path(resource_name), :class=> "buttonBlue"
